# ============================================
# NOMA - Environment Variables
# ============================================

# Turso Database Configuration (Required)
# Get these from: https://turso.tech/app
TURSO_DATABASE_URL=libsql://your-database-name.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token

# Cloudinary Configuration (for media storage)
# Get these from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
CLOUDINARY_URL=cloudinary://api-key:api-secret@cloud-name

# SMTP Email Configuration (for verification emails)
# All emails share the same SMTP server settings
SMTP_HOST=mail.nomaworld.co.ke
SMTP_PORT=587
SMTP_PASS=your-shared-email-password-here

# Email Addresses (comma-separated for multiple addresses)
# These emails share the same password on the mail server
SMTP_EMAIL_INFO=info@nomaworld.co.ke
SMTP_EMAIL_SUPPORT=support@nomaworld.co.ke
SMTP_EMAIL_NOREPLY=noreply@nomaworld.co.ke

# Default sender email (used for verification, OTP, etc.)
SMTP_DEFAULT_SENDER=info@nomaworld.co.ke

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Session Secret (for cookie signing)
SESSION_SECRET=your-secret-key-change-in-production

